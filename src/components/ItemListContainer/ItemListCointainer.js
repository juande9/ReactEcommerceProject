const ItemListContainer = ({ greeting }) => {
    return (
        <section>
            <h1 style={{ padding: 20 , textDecoration: "underline" }}>{greeting}</h1>
        </section>
    )
}

export default ItemListContainer